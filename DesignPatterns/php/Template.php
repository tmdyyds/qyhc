<?php
/*
模板方法模式它在超类中定义了一个算法(业务逻辑)的框架， 允许子类在不修改结构的情况下重写算法的特定步骤。

TIP1:
    模板模式主要作用复用和扩展
*/
####Redis底层数据结构(简单动态字符串、双向链表、压缩链表、哈希表、跳表、整数数组)
哈希表: 查找快,插入大量数据会有hash冲突(hash冲突会造成链式hash,链式hash查找数据只能一个一个通过next指针往下查找,效率开始低下),

####Redis支持类型(String、Hash、列表、集合、有序集合)
String: 简单动态字符串
Hash:
List
SortSet
Set



